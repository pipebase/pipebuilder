base:
  node:
    id: repository0
    role: Repository
    internal_address: 127.0.0.1:17000
  lease:
    ttl: 30
  register:
    endpoints: ["localhost:2379"]
repository:
  manifest: e2e/data/manifests
  app: e2e/data/apps
  catalog_schema: e2e/data/catalog-schema
  catalogs: e2e/data/catalogs
